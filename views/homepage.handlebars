{{#each blog as |blog| }}
<div>
    <div>
    <h3>{{blog.title}}</h3>
    <p>Date posted: {{blog.date_created}}</p>
    <p>Posted by: {{user.name}}</p>
    </div>

    <div>
        <p>{{blog.description}}</p>
    </div>
</div>
{{/each}}